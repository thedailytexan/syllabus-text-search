# Fetched source documents - large, regenerable, never committed
data/*

# ...except small work lists that are findings in their own right and cost real
# time to reproduce. These describe the corpus rather than containing it.
!data/multicolumn_ids.json

# Secrets
.env
.env.*
!.env.example

# Python
__pycache__/
*.py[cod]
.venv/
